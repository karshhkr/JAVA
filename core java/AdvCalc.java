public class AdvCalc extends Calc {  // through the extends keyword we Calc we inherit the properties of AdvClac To Class Of Calc
    // AdvCalc = subclass 
    // Calc = superclass
        public int multi(int k , int l)
    {
        return k*l;
    }
     public int div(int s, int d){
        return s/d;

     }
}
