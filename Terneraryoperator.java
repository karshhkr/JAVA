public class Terneraryoperator {
    public static void main(String[] args)
     {
        int x = 8;
         int result = 0;
      

         // ternary operator is ? and : which means that the ? checks its true and : is used is used to check if false tehn execute after colon//
         
         // in which the  conditions is true return 10 and if false return 20 //
         result = x%2==0 ? 10 : 20;

         System.out.println(result);

         
    }
}
